# Claude Entry

Authoritative rules: [allincms-content-ops/SKILL.md](allincms-content-ops/SKILL.md). Do not restate or fork rules here.

Before any action:

1. Mode-select per SKILL.md → **Mode Selection** (light vs full).
2. STOP if [PROJECT_INDEX.md](PROJECT_INDEX.md) → **Current Site** has any empty field. Request values; never proceed with placeholders.
3. Run Workflow step 0 (sweep [wiki/lessons.md](wiki/lessons.md) for `status: approved` and merge).

Full-mode publish gates: see SKILL.md → **Hard Gates**. Light-mode runbook: see SKILL.md → **Mode Selection**.

If Claude Code-specific browser tools are unavailable, use the same SOP manually through the logged-in browser.
